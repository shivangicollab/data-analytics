22/04/26
- Standard comparison operators like = or <> do not work with NULL. You must use specific operators: 
  IS NULL: Filters for rows where data is missing.
  SELECT * FROM users WHERE email IS NULL;
  IS NOT NULL: Filters for rows with actual data.
  SELECT * FROM users WHERE email IS NOT NULL;

-To count the number of characters in a particular column (cell) we use char_length(content) and not count(content)

23/04/26
-To Count X where Y did NOT happen in a query (LC - 1581 Customer Who Visited but Did Not Make Any Transactions)
  FROM X
  LEFT JOIN Y
  ON condition
  WHERE Y.id IS NULL
  GROUP BY something
  COUNT(X.id)

24/04/26
- I cannot do (Date-1) to compare 2 dates. There are other functions DATE_SUB(allias.date, INTERVAL 1 DAY) DATE_ADD(allias.date, INTERVAL 1 DAY) to acess yesterday and tomorrow
-FROM WEATHER A JOIN WEATHER B is same as FROM WEATHER AS A JOIN WEATHER AS B.
  Once the alliases are formed I cannot use weather.id , use A.id
-ON is used as the criteria to join the table and WHERE is used to filter the data



















